/*
Browser = JS engine (v8, spiderMonkey) + webAPIs 

JS engine contains the call stack where the actual JS code execution happens. 

webAPIs are accessed through the window keyword that is provided by the browser. 

webAPIs - 
DOM APIs - document.getElementById, document.getElementByTagName...
Network APIs - fetch, XHR
UI APIs - canvas, SVG
communication APIs - webSockets, webRTC...
Timer APIs - setTimeout, setInterval
storage APIs - localStorage, sessionStorage
*/

